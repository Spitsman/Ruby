require 'spec_helper'

describe DictionaryApi do
  it 'has a version number' do
    expect(DictionaryApi::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
